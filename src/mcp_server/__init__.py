"""NLOps MCP Server — exposes customer's private tools to AWS DevOps Agent.

Implements the JSON-RPC 2.0 subset of the Model Context Protocol that
DevOps Agent expects (Streamable HTTP transport).
"""
