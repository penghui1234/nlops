# NLOps runtime dependencies (Python 3.12)

# AWS SDK
boto3>=1.34.0,<2.0.0
botocore>=1.34.0,<2.0.0

# Templating
Jinja2>=3.1.4,<4.0.0

# Strands Agents SDK — installed in Lambda via Layer (see infra/nlops_stack.py)
# strands-agents>=1.40.0  # uncomment for local testing

# Optional: MCP client (for path 4 mcp-in fallback, not used in v3)
# mcp>=0.1.0

# Dev / test only
pytest>=8.0.0
pytest-asyncio>=0.23.0
